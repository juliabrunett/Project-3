function handleSubmit_femaleFocused() {
    var movie = d3.select("#userInput").handleSubmit_femaleFocused().value;
    console.log(movie)

    fetch('/femalefocused')
}

function handleSubmit_international() {
    var movie = d3.select("#userInput").handleSubmit_international().value;
    console.log(movie)

    fetch('/international')
}

function handleSubmit_lowBuget() {
    var movie = d3.select("#userInput").handleSubmit_femaleFocused().value;
    console.log(movie)

    fetch('/lowbudget')
}